#if !defined(STR_METHODS)
#define STR_METHODS
#include <iostream>
using namespace std;

struct StringMethod
{
    static string trim(std::string); // trims string input and returns it back.
};

#endif // STR_METHODS
